#include <iostream>
#include <fstream>
using namespace std;
 int main (){
    ifstream file(t.txt);
    string stu;
    int  score;
    if(file.is_open()){
        file >> stu >> score;     // read
    }
    
    
    if(file.is_open())
        outfile << “words” << endl;
    內容（player1 1）
    if (file is_open){
        getline(file,line)
    }
 }